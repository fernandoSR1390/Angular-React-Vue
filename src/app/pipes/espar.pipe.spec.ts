/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EsparPipe } from './espar.pipe';

describe('Pipe: Espare', () => {
  it('create an instance', () => {
    let pipe = new EsparPipe();
    expect(pipe).toBeTruthy();
  });
});
